<resources>

    <style name="AppTheme" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="searchViewStyle">@style/SearchViewStyle</item>

        <item name="android:textColorHighlight">#99ff4081</item>


        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

        <item name="android:windowContentOverlay">@null</item>
<!--
        <item name="list_selector">@drawable/list_selector</item>

        <item name="title_text">@android:color/black</item>
        <item name="selection_text">@color/colorPrimary</item>
        <item name="body_text">#303030</item>
        <item name="drawer_background">@color/primaryBackground</item>-->
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Empty -->
    </style>

    <style name="ToolbarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Empty -->
    </style>

</resources>
